{{!< default}}
<main>
    <article class="{{post_class}}">
        {{#post}}
            <header>
                <h1>{{{title}}}</h1>
                <h5>
                    <time datetime="{{date format='YYYY-MM-DD'}}">publié le {{date format="DD/MM/YYYY"}}</time>
                </h5>
                {{#foreach tags}}
                <a href="{{url}}"><span class="label {{#if @odd}}label-primary{{else}}label-default{{/if}}"><i class="glyphicon glyphicon-tag"></i> {{name}}</span></a>
                &nbsp;
                {{/foreach}}
            </header>

            <section class="posts">
                {{content}}

                <hr />
            </section>
        {{/post}}
    </article>
</main>
